mixin select(data)
  .select
    select
      each item in data
        option= item

mixin select-icons(data)
  .select
    +icons('#ic_gps', 16)
    select
      each item in data
        option= item

mixin select-fill(data)
  .select.select-fill
    select
      each item in data
        option= item